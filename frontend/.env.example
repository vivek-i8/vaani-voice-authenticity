# Copy to .env (local) or set in your hosting dashboard (production).
VITE_API_BASE_URL=http://127.0.0.1:8000

# Local development with the vite dev server: the URL above points at the
# FastAPI backend (uvicorn app.main:app on port 8000).
# Production (Cloudflare Pages): set this to the public backend URL.
